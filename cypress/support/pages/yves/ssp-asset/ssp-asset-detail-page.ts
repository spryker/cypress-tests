import { YvesPage } from '@pages/yves';
import { SspAssetRepository } from './ssp-asset-repository';

export class SspAssetDetailPage extends YvesPage {
  // assertPageLocation(): void {
  //     cy.location('pathname').should('include', this.route);
  // }
  // assertAssetDetails(details: SsAsset & { reference?: string }): void {
  //     if (details.reference) {
  //         cy.get(this.repository.getAssetDetailsReference(details.reference)).should('exist');
  //     }
  //
  //     if (details.name) {
  //         cy.get(this.repository.getAssetDetailsName(details.name)).should('exist');
  //     }
  // }
}
