{
  "name": "cypress-spryker-poc",
  "version": "0.1.0",
  "description": "",
  "main": "index.js",
  "author": "dmiseev@gmail.com",
  "license": "MIT",
  "scripts": {
    "lint": "eslint . --ext .ts",
    "prettier:check": "npx prettier . --check",
    "prettier:write": "npx prettier . --write",
    "cy:open": "cypress open",
    "cy:run": "cypress run --headless --browser chrome",
    "test:checkout": "npx cypress run --spec \"cypress/e2e/checkout/*\" --headless --browser chrome",
    "test:comment": "npx cypress run --spec \"cypress/e2e/comment/*\" --headless --browser chrome",
    "test:merchant-agent-assist": "npx cypress run --spec \"cypress/e2e/merchant-agent-assist/*\" --headless --browser chrome",
    "test:order-management": "npx cypress run --spec \"cypress/e2e/order-management/*\" --headless --browser chrome"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=10.0.0"
  },
  "devDependencies": {
    "@faker-js/faker": "^8.3.1",
    "@types/dotenv": "^8.2.0",
    "@typescript-eslint/eslint-plugin": "^6.13.1",
    "@typescript-eslint/parser": "^6.13.1",
    "cypress": "^13.6.3",
    "eslint": "^8.55.0",
    "inversify": "^6.0.2",
    "prettier": "3.1.0",
    "reflect-metadata": "^0.2.0",
    "typescript": "^5.3.3"
  }
}
